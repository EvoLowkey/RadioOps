// Local development fallback. Vercel rewrites this URL to /api/runtime-config.
// For local use, copy config.example.js values here. These are PUBLIC client values only.
window.RADIOOPS_CONFIG = window.RADIOOPS_CONFIG || {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
